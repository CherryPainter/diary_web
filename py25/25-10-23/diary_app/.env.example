# 将此文件复制为 .env 并填写实际值
# MySQL 连接配置（推荐使用）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DB=diary_db
MYSQL_USER=diary_user
MYSQL_PASSWORD=your_strong_password_here

# 可选：直接提供完整数据库URL（优先级更高）
# DATABASE_URL=mysql+pymysql://diary_user:your_strong_password_here@127.0.0.1:3306/diary_db?charset=utf8mb4

# Flask 密钥（用于会话与CSRF），请替换为足够随机的字符串
SECRET_KEY=replace_with_a_random_long_string

# 登录限流（例如同IP每分钟最多10次）
LOGIN_RATE_LIMIT=10 per minute